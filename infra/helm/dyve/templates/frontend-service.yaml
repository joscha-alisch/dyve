apiVersion: v1
kind: Service
metadata:
  name: {{ include "dyve.fullname" . }}-frontend
  labels:
{{ include "dyve.labels" . | indent 4 }}
spec:
  selector:
    dyve: frontend
{{ include "dyve.labels" . | indent 4 }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
